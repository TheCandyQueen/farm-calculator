function multiply() {
    var answer = Number(document.getElementById("firstno").value) *
        Number(document.getElementById("firstno").value)
    window.alert("Area = " + answer)
  }

  function perimiter() {
      var perimiterAnswer = Number(document.getElementById("secondno").value) * (4)
      window.alert("Perimiter = " + perimiterAnswer)
  }